pipeline {
    agent any

    stages {
        // stage('Cloning Git') {
        //   steps {
        //     git([url: 'https://github.com/KHEfreiIntervenant/it_for_managers_backend.git/', branch: 'main'])
    
        //   }
        // }
        stage('NPM Build') {
          steps {
            sh "echo hello world"
            // sh "nohup npm start &"
          }
        }
    }
}